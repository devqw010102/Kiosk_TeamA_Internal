<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.hcnc.mapper.kiosk.TransportMapper">

	<select id="selectTransportList" resultType="TransportVO">
		SELECT TRANSPORT_ID,
               TYPE,
               DEPART_LOCATION,
               DESTINATION,
               DEPART_TIME,
               ARRIVE_TIME,
               CREATED_AT,
               UPDATED_AT,
               DEL_YN
          FROM TRANSPORT
         WHERE DEL_YN = 'N'
         ORDER BY TYPE, DEPART_TIME, TRANSPORT_ID
	</select>
</mapper>
